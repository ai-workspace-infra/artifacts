apiVersion: v1
kind: Service
metadata:
  name: {{ include "qmd.fullname" . }}
  labels:
    app: qmd
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: qmd
