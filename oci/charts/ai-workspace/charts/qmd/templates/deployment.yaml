apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "qmd.fullname" . }}
  labels:
    app: qmd
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: qmd
  template:
    metadata:
      labels:
        app: qmd
    spec:
      containers:
        - name: qmd
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
