apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-memory-hub.fullname" . }}
  labels:
    app: x-memory-hub
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: x-memory-hub
