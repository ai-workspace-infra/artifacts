apiVersion: v1
kind: Service
metadata:
  name: {{ include "observability.fullname" . }}
  labels:
    app: observability
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: observability
